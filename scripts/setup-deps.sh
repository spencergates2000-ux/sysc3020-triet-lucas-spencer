#!/usr/bin/env bash
# Not needed for JPacman: it is a self-contained Gradle project with no
# companion libraries. Just run `./gradlew build`. (Kept as a no-op.)
echo "JPacman is self-contained — no dependency setup needed. Run: ./gradlew build"
